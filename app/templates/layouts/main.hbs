<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <title>{{title}} - Goofeat</title>
    <style>
        /* Reset CSS */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
            line-height: 1.6;
            color: #333;
            background-color: #f4f4f4;
            margin: 0;
            padding: 0;
            -webkit-text-size-adjust: 100%;
            -ms-text-size-adjust: 100%;
        }
        
        /* Container principal */
        .email-container {
            max-width: 600px;
            width: 100%;
            margin: 0 auto;
            background-color: #ffffff;
            border-radius: 12px;
            overflow: hidden;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
            margin-top: 20px;
            margin-bottom: 20px;
        }
        
        /* Header avec dégradé moderne */
        .email-header {
            background: linear-gradient(120deg, #ff6b35 0%, #ff9a5a 100%);
            color: white;
            padding: 30px 20px;
            text-align: center;
        }
        
        .email-header .logo {
            font-size: 32px;
            font-weight: bold;
            margin-bottom: 5px;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 10px;
        }
        
        .email-header .logo-icon {
            font-size: 38px;
        }
        
        .email-header .tagline {
            font-size: 16px;
            opacity: 0.9;
            font-weight: 300;
            letter-spacing: 0.5px;
        }
        
        /* Corps de l'email */
        .email-body {
            padding: 35px 30px;
        }
        
        .email-title {
            color: #2c3e50;
            font-size: 26px;
            margin-bottom: 25px;
            text-align: center;
            font-weight: 700;
        }
        
        .email-content {
            color: #444;
            font-size: 16px;
            line-height: 1.8;
            margin-bottom: 30px;
        }
        
        .email-content p {
            margin-bottom: 16px;
        }
        
        /* Mise en évidence */
        .highlight {
            background: linear-gradient(120deg, rgba(255, 180, 142, 0.3) 0%, rgba(255, 107, 53, 0.2) 100%);
            border-radius: 4px;
            padding: 3px 6px;
            font-weight: 600;
            color: #d35400;
        }
        
        /* Bouton CTA */
        .cta-section {
            text-align: center;
            margin: 30px 0;
            padding: 5px;
        }
        
        .cta-button {
            display: inline-block;
            background: linear-gradient(120deg, #ff6b35 0%, #ff8c42 100%);
            color: white;
            padding: 15px 35px;
            text-decoration: none;
            border-radius: 50px;
            font-weight: 600;
            font-size: 16px;
            transition: all 0.3s ease;
            box-shadow: 0 4px 15px rgba(255, 107, 53, 0.3);
            letter-spacing: 0.3px;
        }
        
        /* Boîte d'information */
        .info-box {
            background: #f8f9fa;
            border-left: 4px solid #ff6b35;
            padding: 20px;
            margin: 25px 0;
            border-radius: 6px;
            box-shadow: 0 2px 5px rgba(0,0,0,0.05);
        }
        
        /* Liste de fonctionnalités */
        .feature-list {
            background: #f8f9fa;
            padding: 22px;
            border-radius: 8px;
            margin: 25px 0;
            border: 1px solid #eaeaea;
        }
        
        .feature-item {
            margin: 12px 0;
            padding-left: 28px;
            position: relative;
            color: #444;
        }
        
        .feature-item::before {
            content: "✓";
            color: #ff6b35;
            font-weight: bold;
            position: absolute;
            left: 0;
            font-size: 18px;
        }
        
        /* Footer avec meilleur contraste et visibilité */
        .email-footer {
            background: #2c3e50;
            color: #fff;
            padding: 30px 20px;
            text-align: center;
            clear: both;
            display: block;
            width: 100%;
        }
        
        .email-footer .company-info {
            color: #ecf0f1;
            font-size: 15px;
            margin-bottom: 15px;
        }
        
        .email-footer .social-links {
            margin: 20px 0;
        }
        
        .email-footer .social-links a {
            color: #ff8c42;
            text-decoration: none;
            margin: 0 12px;
            font-size: 14px;
            font-weight: 500;
        }
        
        .email-footer .disclaimer {
            color: #bdc3c7;
            font-size: 13px;
            line-height: 1.5;
            margin-top: 20px;
        }
        
        .email-footer .disclaimer a {
            color: #ff8c42;
            text-decoration: underline;
        }
        
        /* Ajustements mobile */
        @media (max-width: 600px) {
            .email-container {
                width: 100% !important;
                margin-top: 0;
                margin-bottom: 0;
                border-radius: 0;
            }
            
            .email-header, 
            .email-body, 
            .email-footer {
                padding: 20px 15px;
            }
            
            .email-title {
                font-size: 22px;
            }
            
            .cta-button {
                width: 100%;
                padding: 15px 20px;
            }
            
            .feature-list {
                padding: 15px;
            }
        }
    </style>
</head>
<body>
    <div class="email-container">
        <!-- Header -->
        <div class="email-header">
            <div class="logo">
                <span class="logo-icon">🍽️</span> Goofeat
            </div>
            <div class="tagline">Mangez mieux, gaspillez moins</div>
        </div>
        
        <!-- Body -->
        <div class="email-body">
            {{#if title}}
                <h1 class="email-title">{{title}}</h1>
            {{/if}}
            
            <div class="email-content">
                {{> @partial-block}}
            </div>
            
            {{#if ctaText}}
                <div class="cta-section">
                    <a href="{{ctaUrl}}" class="cta-button">{{ctaText}}</a>
                </div>
            {{/if}}
            
            {{#if secondaryContent}}
                <div class="email-content">
                    {{{secondaryContent}}}
                </div>
            {{/if}}
        </div>
        
        <!-- Footer avec meilleure visibilité -->
        <div class="email-footer">
            <div class="company-info">
                <strong>Goofeat</strong><br>
                Votre assistant intelligent pour gérer votre garde-manger
            </div>
            
            <div class="social-links">
                <a href="{{frontendUrl}}/contact">Support</a>
                <a href="{{frontendUrl}}/privacy">Confidentialité</a>
                <a href="{{frontendUrl}}/terms">Conditions</a>
            </div>
            
            <div class="disclaimer">
                {{#if unsubscribeUrl}}
                    <p><a href="{{unsubscribeUrl}}">Se désabonner</a></p>
                {{/if}}
                <p>Si vous n'êtes pas à l'origine de cette action, vous pouvez ignorer cet email en toute sécurité.</p>
                <p> Goofeat - {{currentYear}}</p>
            </div>
        </div>
    </div>
</body>
</html>